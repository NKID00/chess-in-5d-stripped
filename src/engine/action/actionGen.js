const deepcopy = require('deep-copy');
const stateUtils = require('@local/engine/stateUtils');
const moveGen = require('@local/engine/move/moveGen');

const possibleActionGen = (gameState, removeDupe = false, allowChecks = false) => {
  var res = [];
  return res;
}
